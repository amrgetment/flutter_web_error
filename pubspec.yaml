name: flutter_web_error
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  custom_lint: ^0.6.4
  solid_lints: ^0.1.5

flutter:
  uses-material-design: true
